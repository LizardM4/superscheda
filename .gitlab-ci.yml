pages:
  stage: deploy
  before_script:
    - sed -e "s/%REV/${CI_COMMIT_SHA:0:7}/g" -i index.html
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
